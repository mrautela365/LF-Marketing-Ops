# Survey Workflow Requirements
#
# This is a standalone service that depends on the parent emailcreationskill service.
# It reuses all parent backend logic: asana_tools, agent, hubspot_tools, etc.
#
# Ensure BOTH services are set up:
#   1. cd ../emailcreationskill && pip install -r requirements.txt
#   2. cd ../survey_workflow && pip install -r requirements.txt
#
# Survey Workflow itself adds NO NEW dependencies — it only orchestrates
# existing tools from the parent emailcreationskill service.

# Shared with emailcreationskill (listed for reference; install from parent/requirements.txt)
requests>=2.28.0
fastapi>=0.100.0
uvicorn>=0.23.0
python-dotenv>=1.0.0
pydantic>=2.0.0
anthropic>=0.7.0
aiohttp>=3.8.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
pillow>=9.0.0
nest-asyncio>=1.5.0
